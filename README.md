# Moxymind frontend interview test for automatization

For running test user needs to:
1. Clone repository 'https://github.com/idRoman/MoxyFrontend1.git'
2. In terminal use 'npm install' all packages required
3. In terminal use 'npm test' to open and run headless cypress
