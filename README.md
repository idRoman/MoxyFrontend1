# Moxymind frontend interview test for automatization

For running test user needs to:
1. Install node.js http://nodejs.org/en/download
2. Clone repository 'https://github.com/idRoman/MoxyFrontend1.git'
3. In terminal use 'npm install' all packages required
4. In terminal use 'npm test' to open and run headless cypress
